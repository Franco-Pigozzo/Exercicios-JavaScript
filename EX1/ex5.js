// Função que retorna a soma de dois números

    function somarNumeros(numero1, numero2) {
        var soma = numero1 + numero2;
        return soma;
    }

// Exemplo de uso da função

    var resultado = somarNumeros(5, 10);
    console.log("A soma é: " + resultado);